import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const LoginScreen = ({navigation}) => {
    return (
        <View>
            <Text>Tjababababababba</Text>
        </View>
    )
}

export default LoginScreen

const styles = StyleSheet.create({})
